<!DOCTYPE html>
<html lang="en">
<head>
  <!-- 
    normalize.css remueve las diferenecia que hay entre browsers segun los defaults de css predeterminados que utilizan. 
    Revisa: http://necolas.github.com/normalize.css/
  -->
  <link rel="stylesheet" href="/css/normalize.css">
  <!--
    application.css este es el archivo donde van tus estilos, tu css
  -->
  <link rel="stylesheet" href="/css/application.css">

  <script src="//ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>
  <script src="/js/application.js"></script>

  <title></title>
</head>
<body>
  <!-- code for declare messages for login, register or logout -->
  <div class="messages">
    <% if session[:error] %>
      <p><%= session[:error] %></p>
      <% session[:error].clear %>
    <% end %>
  </div>
  <div class="messages">
    <% if session[:logoutm] %>
      <p><%= session[:logoutm] %></p>
      <% session[:logoutm].clear %>
    <% end %>
  </div>
  <div class="messages">
    <% if session[:incorrect_login] %>
      <p><%= session[:incorrect_login] %></p>
      <% session[:incorrect_login].clear %>
    <% end %>
  </div>
  <div class="messages">
    <% if session[:successfull] %>
      <p><%= session[:successfull] %></p>
      <% session[:successfull].clear %>
    <% end %>
  </div>
    <div class="messages">
    <% if session[:error_message_register] %>
      <p><%= session[:error_message_register] %></p>
      <% session[:error_message_register].clear %>
    <% end %>
  </div>
  <div class="container">
    <%= yield %>
  </div>
</body>
</html>
